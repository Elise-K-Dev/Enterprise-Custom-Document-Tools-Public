# Web Service

FastAPI tool server for fetching a user-provided URL and converting the page body to Markdown.

This service is intentionally not a search engine. The caller must provide the URL directly.
